/**
 * Pass 17 (audit AI) — DEAD CODE STUB.
 *
 * The previous content of this file (229 lines: `TestShopLocationRecord`
 * type, `FOCUS_PROFILES` constant, `buildNYMetroTestShop` helper,
 * `NY_METRO_TEST_SHOP_LOCATIONS`, `NY_METRO_TEST_SHOPS`, and
 * `NY_METRO_SUGGESTED_ORIGINS` exports) was confirmed dead-code across
 * Pass 11 (initial flag) + co-worker AI Pass 15 sweep + Pass 17
 * independent verification. No source-tree consumers; no test coverage;
 * no barrel re-exports; no external imports of this file.
 *
 * The companion file `nyMetroTestHubSeedData.ts` is ALIVE (consumed by
 * its own test) and unaffected by this stub-out.
 *
 * Cowork-mount permissions block `rm` from this session. Host-side
 * cleanup recommended:
 *   `rm src/app/services/intelligence/nyMetroTestHubSeed.ts`
 *
 * Until then, this stub keeps TypeScript module resolution clean and
 * prevents bundlers from importing the dead exports.
 */
export {};
